# Banking System

Курсов проект по ООП (10 клас, профил програмиране). Конзолно банково приложение на C++.

Проектът се предава на **2 чекпойнта** + финално предаване.

---

## Чекпойнт 1 — План

### Планирани функционалности (8)

1. **CRUD управление на клиенти** — Създаване, преглед, редакция и изтриване на клиентски профили. Всеки клиент има уникален идентификатор, имена, ЕГН, имейл и телефон.
2. **CRUD управление на сметки** — Всеки клиент може да притежава една или повече сметки. Сметките имат IBAN, баланс, валута и статус (активна / блокирана / закрита).
3. **Депозиране и теглене** — Клиентите могат да внасят и изтеглят пари от своята сметка. Валидира се дали има достатъчно средства при теглене и дали сметката е активна.
4. **Преводи между сметки** — Вътрешни преводи (между сметки в системата) и външни преводи (към IBAN извън системата). Без превалутиране, само в евро.
5. **История на транзакциите** — Всяка операция се записва с дата, час, тип, сума и описание. Историята е разделена по сметка и тип.
6. **Генериране на извлечение (Statement)** — Извлечение за избрана сметка с начален / краен баланс и списък на всички движения.
7. **Управление на карти** — Към сметка могат да се издават дебитни или виртуални карти с номер, срок на валидност и CVV (маскиран). Поддържа блокиране и деблокиране.
8. **Роли и права (Authorization)** — Системата поддържа клиент и администратор. Администраторът може да блокира сметки и да управлява потребителски данни; клиентът вижда само собствените си данни.

### Планирана класова йерархия

```
Person (абстрактен)
 ├── Customer
 └── Admin

Account
Transaction
Bank   (мениджър — съдържа vector<Customer>, vector<Account>, vector<Transaction>)
```

---

## Чекпойнт 2 — Прогрес

Изискването на чекпойнт 2 е **минимум 4 от 8** функционалности. Работата беше разделена на дни.

### Ден 1 — Класова йерархия + базов CRUD ✅

- ✅ Абстрактен базов клас `Person` с чисто виртуален метод `role()` и виртуален `print()`.
- ✅ Наследници: `Customer` и `Admin` (`override` на `role()`).
- ✅ Клас `Account` (IBAN, баланс, валута, статус — `enum AccountStatus { ACTIVE, BLOCKED, CLOSED }`).
- ✅ Клас `Bank` — централен мениджър.
- ✅ **CRUD клиенти** (точка 1).
- ✅ **CRUD сметки** (точка 2).
- ✅ Демонстрация на полиморфизъм в менюто.

### Ден 2 — Операции и транзакции ✅

- ✅ Клас `Transaction` (дата, час, тип, сума, описание).
- ✅ **Депозит и теглене** с валидация (точка 3).
- ✅ **История на транзакциите** — общ списък, по сметка, по тип (точка 5).

### Покрити функционалности на чекпойнт 2

| # | Функционалност | Статус |
|---|---|---|
| 1 | CRUD клиенти | ✅ |
| 2 | CRUD сметки | ✅ |
| 3 | Депозит / Теглене | ✅ |
| 5 | История на транзакции | ✅ |
| 4 | Преводи | ⏳ за чекпойнт 3 / финалното |
| 6 | Извлечение | ⏳ за чекпойнт 3 / финалното |
| 7 | Карти | ⏳ за чекпойнт 3 / финалното |
| 8 | Роли и права | ⏳ за чекпойнт 3 / финалното |

**4 от 8** функционалности са имплементирани — отговаря на минималното изискване за чекпойнт 2. Останалите 4 ще бъдат добавени в чекпойнт 3 и финалното предаване.

### Бележки спрямо плана от чекпойнт 1

Йерархията е реализирана както е планирана. Класът `Transaction` е добавен към плана, защото е естествена част от точка 5 (история на транзакциите). Данните се пазят само в паметта.

---

## Билд и стартиране

Изисква `g++` с поддръжка на C++17.

### Вариант 1: с Makefile

```sh
make
./banking
```

`make clean` чисти обектните файлове и изпълнимия.

### Вариант 2: директна команда (без Makefile)

```sh
g++ -std=c++17 -Iinclude src/*.cpp -o banking
./banking
```

---

## Структура на проекта

```
banking-system/
├── include/
│   ├── Person.h        — абстрактен базов клас
│   ├── Customer.h      — наследник: клиент
│   ├── Admin.h         — наследник: администратор
│   ├── Account.h       — клас за сметка + enum AccountStatus
│   ├── Transaction.h   — клас за транзакция
│   └── Bank.h          — мениджър: CRUD, операции, лог
├── src/
│   ├── Person.cpp
│   ├── Customer.cpp
│   ├── Admin.cpp
│   ├── Account.cpp
│   ├── Transaction.cpp
│   ├── Bank.cpp
│   └── main.cpp        — меню и демонстрация
├── Makefile
└── README.md
```

---

## Меню (главни опции)

| # | Действие |
|---|---|
| 1 | Създай клиент |
| 2 | Покажи всички клиенти |
| 3 | Намери клиент по ID |
| 4 | Редактирай клиент |
| 5 | Изтрий клиент (каскадно премахва и неговите сметки) |
| 6 | Създай сметка |
| 7 | Покажи всички сметки |
| 8 | Намери сметка по IBAN |
| 9 | Промени статус на сметка (ACTIVE / BLOCKED / CLOSED) |
| 10 | Изтрий сметка |
| 11 | Сметките на конкретен клиент |
| 12 | Депозит |
| 13 | Теглене |
| 14 | Покажи всички транзакции |
| 15 | История на сметка |
| 16 | История на сметка по тип |
| 17 | Демонстрация на полиморфизъм (`Person*` → `Customer` и `Admin`) |
| 0 | Изход |

При стартиране се зареждат двама демо клиенти и две сметки, за да може менюто да се тества веднага.

---

## ООП принципи в проекта

- **Енкапсулация:** всички полета са `protected` / `private`. Достъп само през getters / setters. Например `balance` в `Account` се променя само през `setBalance` или операции като `deposit` / `withdraw`, които валидират сумата.
- **Наследяване:** `Customer` и `Admin` наследяват `Person` и преизползват общите атрибути. Конструкторите им извикват конструктора на базовия клас.
- **Полиморфизъм:** `virtual std::string role() const = 0;` в `Person` се преизпълнява в `Customer` (връща `"CUSTOMER"`) и `Admin` (връща `"ADMIN"`). Метода `print()` е виртуален и при викане през `Person*` автоматично избира правилния `role()`. Виж функцията `demoPolymorphism()` в `main.cpp` (опция 17).
- **Абстракция:** `Person` е абстрактен клас (заради `= 0`). Не може да се създаде самостоятелен `Person` — само конкретен `Customer` или `Admin`.
